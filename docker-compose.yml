services:
  switchboard:
    build: .
    ports:
      - "8100:8100"
    volumes:
      - ./data:/data
    environment:
      - SWITCHBOARD_DB=/data/switchboard.db
      - SWITCHBOARD_PORT=8100
    restart: unless-stopped
